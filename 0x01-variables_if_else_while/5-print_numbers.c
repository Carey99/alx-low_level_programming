#include <stdio.h>
/**
 * main - this program prints digits of base ten
 * Return:Always 0
 */
int main(void)
{
	int index;

	for (index = 0; index <= 9; ++index)
{
	printf("%d", index);
}
	printf("\n");

	return (0);
}
